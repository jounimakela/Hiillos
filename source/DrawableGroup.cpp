#include "DrawableGroup.h"

DrawableGroup::DrawableGroup(){}

DrawableGroup::~DrawableGroup() {
        /*
        for(std::vector<sf::Drawable*>::iterator it = drawables.begin();
                it != drawables.end();
                ++it)
        {
                //delete *it;
        }
        */
}

void DrawableGroup::render(sf::RenderWindow* window) const {
        /*
        for(std::vector<sf::Drawable*>::iterator it = drawables.begin();
                it != drawables.end();
                ++it)
        {
                //window->draw(*it);
        }
        */
}
